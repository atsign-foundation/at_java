# Java Client SDK Examples

It is intended to illustrate the following:

* Instantiating an AtClient instance
* Invoke the API the put/get/delete "map" like interface for the different
  key types (Public Keys, Self Keys, Shared Keys)

**Note:** This module is NOT published.
